localdev:
  driver: postgres
  open: postgres://postgres:cfssl@localhost/postgres?sslmode=disable

development:
  driver: postgres
  open: dbname=certdb_development sslmode=disable

test:
  driver: postgres
  open: dbname=certdb_test sslmode=disable

staging:
  driver: postgres
  open: dbname=certdb_staging sslmode=disable

production:
  driver: postgres
  open: dbname=certdb_production sslmode=disable
